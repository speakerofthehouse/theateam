<!DOCTYPE html>
<html>
<body>

<form>
<input type="button" value="Shuffle 1" onclick="msg()">
</form>

<p>The button above activates a JavaScript when it is clicked.</p>

<script>
function msg() {
alert("Hello world!");
}
</script>

<select id="volume">

<%
var options = [ "biased", "shuffle", "s1", "s2", "s3", "s4" ];
for ( var i = 0; i < options.length; i++ )
{
var selected = ( config[0].volume == i ) ? "selected" : "";
%><option value="<%=options[ i ] %>" <%=selected %>><%=i %></option><%
}
%>
</select>

</body>
</html>